<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%=project.title%> | Climate Solutions</title>
  <link rel="stylesheet" href="/css/main.css"/>
</head>
<body>
  <%- include('partials/navbar',{ page:`/solutions/projects/${project.id}` }) %>
  <section class="hero bg-base-200 py-12">
    <div class="hero-content flex flex-col md:flex-row justify-between">
      <div>
        <h1 class="text-5xl font-bold"><%=project.title%></h1>
        <p class="py-2"><%=project.description%></p>
      </div>
      <% if (session.user) { %>
        <div class="text-right">
          <p class="font-semibold">
            Logged in as <%=session.user.userName%><br/>
            <span class="opacity-70"><%=session.user.email%></span>
          </p>
        </div>
      <% } %>
    </div>
  </section>
  <main class="container mx-auto py-8 px-4 space-y-6">
    <div class="text-center">
      <img src="<%=project.feature_img_url%>"
           alt="<%=project.title%>"
           class="max-w-lg w-full rounded-lg"/>
    </div>
    <p><%=project.intro_short%></p>
    <p><%=project.impact%></p>
    <p>
      <a href="<%=project.original_source_url%>" target="_blank" class="link">
        Read original source
      </a>
    </p>
    <div class="card-actions justify-end space-x-4">
      <% if (session.user) { %>
        <a href="/solutions/editProject/<%=project.id%>" class="btn btn-success">Edit</a>
      <% } %>
      <a href="/solutions/deleteProject/<%=project.id%>" class="btn btn-error">Delete</a>
      <button onclick="history.back()" class="btn btn-outline">Back to List</button>
    </div>
  </main>
</body>
</html>
